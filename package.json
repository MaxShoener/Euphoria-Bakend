{
  "name": "wisp-proxy-playwright",
  "version": "1.0.0",
  "description": "Headless proxy using Playwright",
  "main": "server.js",
  "scripts": {
    "postinstall": "npx playwright install chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  }
}
