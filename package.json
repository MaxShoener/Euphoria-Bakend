{
  "name": "wisp-proxy-playwright",
  "version": "1.0.0",
  "description": "Headless proxy using Playwright for fetching pages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.43.0"
  }
}
