{
  "name": "euphoria-proxy",
  "version": "1.0.0",
  "description": "Euphoria Proxy using Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.42.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}