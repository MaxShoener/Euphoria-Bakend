{
  "name": "euphoria-proxy",
  "version": "1.0.0",
  "description": "Headless proxy using Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"   // or the latest version
  }
}